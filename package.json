{
  "name": "circuit",
  "version": "1.0.0",
  "description": "Climbing Circuit Logger",
  "main": "index.js",
  "scripts": {
    "start": "cp src/index.html index.html && node tools/string-template-replace.js && react-scripts start",
    "build": "cp src/index.html index.html && NODE_ENV=production node tools/string-template-replace.js && react-scripts build",
    "deploy": "firebase deploy",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "author": "Antoine Grant <antoine.grant@nytimes.com>",
  "license": "ISC",
  "dependencies": {
    "react": "^15.3.2",
    "react-dom": "^15.3.2"
  },
  "devDependencies": {
    "glob": "^7.0.6",
    "node-yaml-config": "0.0.4",
    "react-scripts": "0.4.3",
    "replace": "^0.3.0"
  }
}
